- hosts: all
  become: yes
  roles:
    - move_k8s_repo_to_k8s_specific_dir
  tasks:
    - name: Install python3.7
      shell:
        cmd: |
          set -ex
          add-apt-repository -y ppa:deadsnakes/ppa
          apt update
          apt install -y python3.7
        executable: /bin/bash
