- name: Set fact for kubectl
  set_fact:
    kubectl:
      kubectl: '{{ golang_env.GOPATH }}/src/k8s.io/kubernetes/cluster/kubectl.sh'

- name: Merge kubectl into golang env
  set_fact:
    golang_env: '{{ golang_env | combine(kubectl) }}'
